Read the [official docs](https://example.com/docs) for more info.

A link with a `"title"` attribute: [example](https://example.com "Hover text").

A bare URL: https://example.com/bare — should render as a clickable link
on both platforms.
