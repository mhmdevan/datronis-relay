Here's a Python example:

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"

print(greet("world"))
```

And a shell example:

```bash
curl -fsSL https://example.com/install.sh | bash
```

A code block with no language hint:

```
plain
text
block
```
